import localFont from "next/font/local";

export const inter = localFont({
  src: "../fonts/Inter.ttf", // Change this line if font is in 'src/fonts'
  display: "swap",
  variable: "--font-inter",
});
